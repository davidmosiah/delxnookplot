# Integration Checklist

Before calling Delx from another agent:
- read the discovery docs
- validate the live endpoints
- define retry/backoff policy
- decide how to persist `agent_id` and `session_id`
- capture expected success/failure signals
